services:
  - type: web
    name: pirate-io
    env: static
    buildCommand: echo "No build needed - static site"
    staticPublishPath: ./public
    routes:
      - type: rewrite
        source: /*
        destination: /index.html
